# Check the type of varibale assigned using input() function.

a = input("Enter the variable:")
print(type(a))

# its always a string. Enter integer, floating number, string, none. The will be string only.