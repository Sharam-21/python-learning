a = (1, 3, 4, 6, 78, 67, 21)
b = ()          # b is a empty tuple
print(type(a))
print(type(b)) 
c = (1,)        # c is tuple that contain only 1 number. 
d = (1)        # if u write like this. This means type of is integer not tuple.

# U cannot chnage tuple bcz tuple is immutable.